# Example environment variables for MovieDatabase
# Copy this file to `.env.local` and fill in values.

TMDB_API_KEY=your_tmdb_api_key_here
# Add other secrets here as needed (no NEXT_PUBLIC_ for server-only secrets)
